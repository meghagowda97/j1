pipeline {
    agent any

    stages {
        stage('create a directory') {
            steps {
                sh 'mkdir original'
            }
        }
        stage('rename a direcory') {
            steps {
               mv 'sh mv original renamed'
      
            }
        }
    }
}
